# Ink 1.1.7

Editor chrome from the redesign HTML. PRs go to `release/1.1.7`, then merge to `main` after checks.

## This slice

- Heading dropdown is a light boxed popover portaled to the document (no charcoal overlay, no toolbar clip)
- Grouped toolbar dividers + Ask Ink AI pill
- Slash menu: search row, BASIC / MEDIA groups, descriptions, shortcuts
- Callout, code, quote, image, heading 3, paragraph inserts
- Status bar: words · chars · synced
- Portal Lab (`/lab`) and public Login (`/login`) hidden for 1.1.7 publish

## Backlog

- INK-63 outline rail
- HTML source view
- Further toolbar grouping as first-class `tgroup` nodes
