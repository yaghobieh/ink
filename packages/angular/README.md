# Angular adapter

Public entry: `@forgedevstack/ink/angular`

1.0.0 ships usage helpers and documentation for mounting React `InkEditor` via a bridge. A native `@angular/core` component lands in 1.x.
